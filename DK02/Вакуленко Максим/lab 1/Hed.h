#include<stdio.h> 
#include<stdlib.h>
// Cтворюємо загалні змінні 
#define HED_H
#ifdef HED_H
int summa(int num_b);
double power(int base, int pow);
long factorial(int number);
double q(int A, int B, int C);

     // Прототип функции

#endif // !TEA_H#pragma once
