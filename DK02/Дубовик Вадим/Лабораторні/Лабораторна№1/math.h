#pragma once
#include <stdio.h>
#include <stdlib.h>
#define MATH_H
#ifdef MATH_H

double power(int base, int pow);
long sqrt(int a);
long fctrl(int a);
double Q_is(int A, int B, int C);
#endif // !MATH_H
