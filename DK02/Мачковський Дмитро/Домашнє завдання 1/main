#include <string.h>
#include <ctype.h>
#include "dk_tool.h"

int main()
{
	char *s;
	s = (char*)malloc(50 * sizeof(char));
	printf("Enter the sentence: ");
	gets(s);
	char *seq;
	seq = (char*)malloc(4 * sizeof(char));
	printf("Enter you <= 3 symbols: ");
	gets(seq);
	func(s, seq);
	free(s);
	free(seq);
		
}
